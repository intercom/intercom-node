// This file was auto-generated by Fern from our API Definition.

import type * as Intercom from "../../../index.js";

/**
 * A paginated list of side conversations for a conversation.
 */
export interface SideConversationList {
    /** The type of the response object. */
    type?: "side_conversation.list";
    /** An array of side conversation objects. */
    side_conversations?: Intercom.preview.SideConversationSummary[];
    /** The total number of side conversations. */
    total_count?: number;
    /** Pagination metadata. */
    pages?: SideConversationList.Pages;
}

export namespace SideConversationList {
    /**
     * Pagination metadata.
     */
    export interface Pages {
        type?: "pages";
        /** The current page number. */
        page?: number;
        /** The number of results per page. */
        per_page?: number;
        /** The total number of pages. */
        total_pages?: number;
    }
}
