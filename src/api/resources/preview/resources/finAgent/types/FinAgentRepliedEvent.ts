// This file was auto-generated by Fern from our API Definition.

/**
 * Event fired when Fin replies to a user.
 * The content of the response will be contained in the message object.
 * Intermediate replies have status 'replying'; a separate fin_status_updated
 * event with 'awaiting_user_reply' fires as a done signal.
 */
export interface FinAgentRepliedEvent {
    /** The name of the event. */
    event_name: "fin_replied";
    /** The ID of the conversation. */
    conversation_id: string;
    /** The ID of the user. */
    user_id: string;
    /** Fin's answer to the user's query. */
    message: FinAgentRepliedEvent.Message;
    /**
     * Fin's current status.
     * - replying: Intermediate reply part; more parts may follow
     * - awaiting_user_reply: Legacy status; use fin_status_updated event as the done signal instead
     */
    status: FinAgentRepliedEvent.Status;
    /**
     * Optional. Present when the reply was generated via streaming.
     * Correlates this event with the fin_reply_chunk events that preceded it.
     * Use this to know when to replace streamed chunk_text with the final message body.
     */
    stream_id?: string;
    /** The timestamp the event was created at, with millisecond precision. */
    created_at_ms: string;
}

export namespace FinAgentRepliedEvent {
    /**
     * Fin's answer to the user's query.
     */
    export interface Message {
        /** A unique identifier for this message. */
        id?: string;
        /** The author of the message (always 'fin' for this event). */
        author: "fin";
        /** The HTML body of Fin's response. */
        body: string;
        /** The timestamp the message was created at, with millisecond precision. */
        timestamp_ms: string;
    }

    /**
     * Fin's current status.
     * - replying: Intermediate reply part; more parts may follow
     * - awaiting_user_reply: Legacy status; use fin_status_updated event as the done signal instead
     */
    export const Status = {
        Replying: "replying",
        AwaitingUserReply: "awaiting_user_reply",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
