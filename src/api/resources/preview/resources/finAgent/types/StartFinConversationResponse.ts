// This file was auto-generated by Fern from our API Definition.

import type * as Intercom from "../../../../../index.js";

export interface StartFinConversationResponse {
    /** The ID of the conversation. */
    conversation_id?: string;
    /** The ID of the user. */
    user_id?: string;
    /** Fin's current status in the conversation workflow. */
    status?: StartFinConversationResponse.Status;
    /** The timestamp the response was created at, with millisecond precision. */
    created_at_ms?: string;
    errors?: Intercom.preview.FinAgentAttributeErrors;
    /** Optional. A URL to subscribe to Server-Sent Events (SSE) for this conversation, if SSE is enabled. The access token is a JWT with a 3-minute TTL. The token is revoked when Fin sets the conversation to awaiting_user_reply or complete status. */
    sse_subscription_url?: string;
}

export namespace StartFinConversationResponse {
    /** Fin's current status in the conversation workflow. */
    export const Status = {
        Thinking: "thinking",
        Replying: "replying",
        AwaitingUserReply: "awaiting_user_reply",
        Escalated: "escalated",
        Resolved: "resolved",
        Complete: "complete",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
