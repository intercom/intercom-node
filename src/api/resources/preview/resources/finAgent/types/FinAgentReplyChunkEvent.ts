// This file was auto-generated by Fern from our API Definition.

/**
 * SSE-only event fired during streaming reply generation.
 * Each chunk contains the full accumulated plain text of Fin's answer so far (cumulative, not a delta).
 * Only delivered over SSE when streaming is enabled. Not available via webhooks.
 * When the fin_replied event arrives with the same stream_id, replace streamed text with the final HTML body.
 */
export interface FinAgentReplyChunkEvent {
    /** The name of the event. */
    event_name: "fin_reply_chunk";
    /** The ID of the conversation. */
    conversation_id: string;
    /**
     * A unique identifier for this streaming response.
     * Correlates chunks with each other and with the eventual fin_replied event.
     */
    stream_id: string;
    /** 0-based counter for this chunk within the stream. Contiguous. */
    chunk_index: number;
    /**
     * The full accumulated plain text of Fin's answer so far.
     * Each chunk supersedes the previous — replace rather than append.
     */
    chunk_text: string;
    /** Fin's current status (always 'replying' for this event). */
    status?: "replying";
    /** The timestamp the event was created at, with millisecond precision. */
    created_at_ms: string;
}
