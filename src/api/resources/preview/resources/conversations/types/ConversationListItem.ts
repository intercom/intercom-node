// This file was auto-generated by Fern from our API Definition.

import type * as Intercom from "../../../../../index.js";

/**
 * The data returned about your conversations when you list or search them.
 */
export interface ConversationListItem {
    /** Always conversation. */
    type?: string;
    /** The id representing the conversation. */
    id?: string;
    /** The title given to the conversation. */
    title?: string;
    /** The time the conversation was created. */
    created_at?: number;
    /** The last time the conversation was updated. */
    updated_at?: number;
    /** The last time a Contact responded to an Admin. In other words, the time a customer started waiting for a response. Set to null if last reply is from an Admin. */
    waiting_since?: number;
    /** If set this is the time in the future when this conversation will be marked as open. i.e. it will be in a snoozed state until this time. i.e. it will be in a snoozed state until this time. */
    snoozed_until?: number;
    /** Indicates whether a conversation is open (true) or closed (false). */
    open?: boolean;
    /** Can be set to "open", "closed" or "snoozed". */
    state?: ConversationListItem.State;
    /** Indicates whether a conversation has been read. */
    read?: boolean;
    /** If marked as priority, it will return priority or else not_priority. */
    priority?: ConversationListItem.Priority;
    /** The id of the admin assigned to the conversation. If it's not assigned to an admin it will return 0. */
    admin_assignee_id?: number;
    /** The id of the team assigned to the conversation. If it's not assigned to a team it will return 0. */
    team_assignee_id?: number;
    /** The company associated with the conversation. */
    company?: Intercom.preview.Company;
    tags?: Intercom.preview.Tags;
    conversation_rating?: Intercom.preview.ConversationRating;
    source?: Intercom.preview.ConversationSource;
    contacts?: Intercom.preview.ConversationContacts;
    teammates?: Intercom.preview.ConversationTeammates;
    custom_attributes?: Intercom.preview.CustomAttributes;
    first_contact_reply?: Intercom.preview.ConversationFirstContactReply;
    sla_applied?: Intercom.preview.SlaApplied;
    statistics?: Intercom.preview.ConversationStatistics;
    linked_objects?: Intercom.preview.LinkedObjectList;
    /** Indicates whether the AI Agent participated in the conversation. */
    ai_agent_participated?: boolean;
    ai_agent?: Intercom.preview.AiAgent;
    /** Indicates whether the Sales Agent participated in the conversation. */
    sales_agent_participated?: boolean;
    sales_agent?: Intercom.preview.SalesAgent;
}

export namespace ConversationListItem {
    /** Can be set to "open", "closed" or "snoozed". */
    export const State = {
        Open: "open",
        Closed: "closed",
        Snoozed: "snoozed",
    } as const;
    export type State = (typeof State)[keyof typeof State];
    /** If marked as priority, it will return priority or else not_priority. */
    export const Priority = {
        Priority: "priority",
        NotPriority: "not_priority",
    } as const;
    export type Priority = (typeof Priority)[keyof typeof Priority];
}
