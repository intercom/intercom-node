// This file was auto-generated by Fern from our API Definition.

import type * as Intercom from "../../../../../../index.js";

/**
 * @example
 *     {
 *         data_connector_id: "12345",
 *         per_page: 1,
 *         starting_after: "starting_after",
 *         success: "true",
 *         error_type: "request_configuration_error",
 *         start_ts: 1,
 *         end_ts: 1,
 *         include_bodies: "true"
 *     }
 */
export interface ListDataConnectorExecutionResultsRequest {
    /** The unique identifier for the data connector. */
    data_connector_id: string;
    /** The number of results per page (1-30, default 10). */
    per_page?: number;
    /** Cursor for pagination. Use the value from `pages.next.starting_after` in a previous response. */
    starting_after?: string;
    /** Filter by success status. Use `true`, `false`, or omit for all. */
    success?: Intercom.preview.ListDataConnectorExecutionResultsRequestSuccess;
    /** Filter by error type. */
    error_type?: Intercom.preview.ListDataConnectorExecutionResultsRequestErrorType;
    /** Unix timestamp for start of time range (default 1 hour ago). */
    start_ts?: number;
    /** Unix timestamp for end of time range (default now). */
    end_ts?: number;
    /** Include request/response bodies in the response (default false). */
    include_bodies?: Intercom.preview.ListDataConnectorExecutionResultsRequestIncludeBodies;
}
