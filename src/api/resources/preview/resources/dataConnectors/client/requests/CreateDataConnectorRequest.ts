// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         name: "Get Order Status",
 *         description: "Looks up order status from an external service",
 *         http_method: "get",
 *         url: "https://api.example.com/orders/{{order_id}}/status",
 *         direct_fin_usage: true,
 *         audiences: ["leads", "visitors"],
 *         headers: [{
 *                 name: "Content-Type",
 *                 value: "application/json"
 *             }],
 *         data_inputs: [{
 *                 name: "order_id",
 *                 type: "string",
 *                 description: "The order ID to look up",
 *                 required: true,
 *                 default_value: ""
 *             }],
 *         customer_authentication: true,
 *         bypass_authentication: false,
 *         validate_missing_attributes: true
 *     }
 *
 * @example
 *     {
 *         name: "My Connector"
 *     }
 *
 * @example
 *     {
 *         name: "Order Lookup",
 *         mock_response: {
 *             "order": {
 *                 "id": 12345,
 *                 "status": "shipped"
 *             }
 *         }
 *     }
 */
export interface CreateDataConnectorRequest {
    /** The name of the data connector. */
    name: string;
    /** A description of what this data connector does. */
    description?: string;
    /** The HTTP method used when calling the external API. */
    http_method?: CreateDataConnectorRequest.HttpMethod;
    /** The URL of the external API endpoint. Supports template variables like `{{order_id}}`. */
    url?: string;
    /** The request body template. Supports template variables. */
    body?: string;
    /** Whether the connector is used directly by Fin (true) or only in workflows (false). Defaults to false. */
    direct_fin_usage?: boolean;
    /** The user types this connector is available for. */
    audiences?: CreateDataConnectorRequest.Audiences.Item[];
    /** HTTP headers to include in the request. */
    headers?: CreateDataConnectorRequest.Headers.Item[];
    /** Input parameters accepted by the connector. */
    data_inputs?: CreateDataConnectorRequest.DataInputs.Item[];
    /** Whether the connector requires customer authentication before executing. Defaults to false. */
    customer_authentication?: boolean;
    /** Whether authentication is bypassed entirely (public endpoint). Defaults to false. */
    bypass_authentication?: boolean;
    /** Whether to validate that all required data inputs have values before executing. */
    validate_missing_attributes?: boolean;
    /** A sample JSON response from the external API. Auto-generates `response_fields` and sets `configuration_response_type` to `mock_response_type`. */
    mock_response?: Record<string, unknown>;
    /** IDs of authentication tokens to attach to this data connector. */
    token_ids?: string[];
}

export namespace CreateDataConnectorRequest {
    /** The HTTP method used when calling the external API. */
    export const HttpMethod = {
        Get: "get",
        Post: "post",
        Put: "put",
        Delete: "delete",
        Patch: "patch",
    } as const;
    export type HttpMethod = (typeof HttpMethod)[keyof typeof HttpMethod];
    export type Audiences = Audiences.Item[];

    export namespace Audiences {
        export const Item = {
            Leads: "leads",
            Users: "users",
            Visitors: "visitors",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    export type Headers = Headers.Item[];

    export namespace Headers {
        export interface Item {
            /** The header name. */
            name?: string;
            /** The header value. Supports template variables. */
            value?: string;
        }
    }

    export type DataInputs = DataInputs.Item[];

    export namespace DataInputs {
        export interface Item {
            /** The parameter name. */
            name?: string;
            /** The parameter type. */
            type?: Item.Type;
            /** A description of the parameter. */
            description?: string;
            /** Whether the parameter is required. */
            required?: boolean;
            /** The default value for the parameter. Defaults to an empty string if omitted. */
            default_value?: string;
        }

        export namespace Item {
            /** The parameter type. */
            export const Type = {
                String: "string",
                Integer: "integer",
                Decimal: "decimal",
                Boolean: "boolean",
            } as const;
            export type Type = (typeof Type)[keyof typeof Type];
        }
    }
}
