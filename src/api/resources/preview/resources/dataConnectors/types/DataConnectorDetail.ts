// This file was auto-generated by Fern from our API Definition.

/**
 * Full detail view of a data connector, returned by `GET /data_connectors/{id}`.
 * Includes configuration, data inputs, response fields, and object mappings.
 */
export interface DataConnectorDetail {
    /** The type of object - `data_connector`. */
    type?: "data_connector";
    /** The unique identifier for the data connector. */
    id?: string;
    /** The name of the data connector. */
    name?: string;
    /** A description of what this data connector does. */
    description?: string;
    /** The current state of the data connector. */
    state?: DataConnectorDetail.State;
    /** The URL of the external API endpoint. Supports template variables like `{{order_id}}`. */
    url?: string;
    /** The request body template. Supports template variables. */
    body?: string;
    /** HTTP headers for the request. Header values are always redacted as `"****"` in responses. */
    headers?: DataConnectorDetail.Headers.Item[];
    /** The HTTP method used by the data connector. */
    http_method?: DataConnectorDetail.HttpMethod;
    /** Whether this connector is used directly by Fin. */
    direct_fin_usage?: boolean;
    /** The audience types this connector targets. */
    audiences?: DataConnectorDetail.Audiences.Item[];
    /** How the connector executes. */
    execution_type?: DataConnectorDetail.ExecutionType;
    /** The expected response format from the connector. */
    configuration_response_type?: DataConnectorDetail.ConfigurationResponseType;
    /** The type of data transformation applied to the response. */
    data_transformation_type?: DataConnectorDetail.DataTransformationType;
    /** The name of the client-side function, if applicable. */
    client_function_name?: string;
    /** Timeout in milliseconds for the client function, if applicable. */
    client_function_timeout_ms?: number;
    /** The input parameters accepted by this data connector. */
    data_inputs?: DataConnectorDetail.DataInputs.Item[];
    /** The fields returned in the connector response. */
    response_fields?: DataConnectorDetail.ResponseFields.Item[];
    /** Mappings from connector response objects to Intercom objects. */
    object_mappings?: DataConnectorDetail.ObjectMappings.Item[];
    /** IDs of authentication tokens associated with this connector. */
    token_ids?: string[];
    /** Whether OTP authentication is enabled for this connector. */
    customer_authentication?: boolean;
    /** Whether authentication is bypassed for this connector. */
    bypass_authentication?: boolean;
    /** Whether to validate missing attributes before execution. */
    validate_missing_attributes?: boolean;
    /** The ID of the admin who created this connector. */
    created_by_admin_id?: string;
    /** The ID of the admin who last updated this connector. */
    updated_by_admin_id?: string;
    /** The time the data connector was created. */
    created_at?: string;
    /** The time the data connector was last updated. */
    updated_at?: string;
    /** The URL path to fetch execution results for this connector. */
    execution_results_url?: string;
}

export namespace DataConnectorDetail {
    /** The current state of the data connector. */
    export const State = {
        Draft: "draft",
        Live: "live",
    } as const;
    export type State = (typeof State)[keyof typeof State];
    export type Headers = Headers.Item[];

    export namespace Headers {
        export interface Item {
            /** The header name. */
            name?: string;
            /** Always `"****"` in responses. */
            value?: string;
        }
    }

    /** The HTTP method used by the data connector. */
    export const HttpMethod = {
        Get: "get",
        Post: "post",
        Put: "put",
        Delete: "delete",
        Patch: "patch",
    } as const;
    export type HttpMethod = (typeof HttpMethod)[keyof typeof HttpMethod];
    export type Audiences = Audiences.Item[];

    export namespace Audiences {
        export const Item = {
            Users: "users",
            Leads: "leads",
            Visitors: "visitors",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    /** How the connector executes. */
    export const ExecutionType = {
        ServerSide: "server_side",
        ClientSide: "client_side",
    } as const;
    export type ExecutionType = (typeof ExecutionType)[keyof typeof ExecutionType];
    /** The expected response format from the connector. */
    export const ConfigurationResponseType = {
        TestResponseType: "test_response_type",
        MockResponseType: "mock_response_type",
    } as const;
    export type ConfigurationResponseType = (typeof ConfigurationResponseType)[keyof typeof ConfigurationResponseType];
    /** The type of data transformation applied to the response. */
    export const DataTransformationType = {
        FullAccess: "full_access",
        RedactedAccess: "redacted_access",
        CodeBlockTransformation: "code_block_transformation",
    } as const;
    export type DataTransformationType = (typeof DataTransformationType)[keyof typeof DataTransformationType];
    export type DataInputs = DataInputs.Item[];

    export namespace DataInputs {
        export interface Item {
            /** The name of the input parameter. */
            name?: string;
            /** The data type of the input. */
            type?: Item.Type;
            /** A description of the input parameter. */
            description?: string;
            /** Whether this input is required. */
            required?: boolean;
            /** The default value for this input, if any. */
            default_value?: string;
        }

        export namespace Item {
            /** The data type of the input. */
            export const Type = {
                String: "string",
                Integer: "integer",
                Decimal: "decimal",
                Boolean: "boolean",
            } as const;
            export type Type = (typeof Type)[keyof typeof Type];
        }
    }

    export type ResponseFields = ResponseFields.Item[];

    export namespace ResponseFields {
        export interface Item {
            /** The JSON path of the response field. */
            path?: string;
            /** The data type of the response field. */
            type?: Item.Type;
            /** An example value for this field. */
            example_value?: unknown;
            /** Whether this field is redacted in logs. */
            redacted?: boolean;
        }

        export namespace Item {
            /** The data type of the response field. */
            export const Type = {
                Unknown: "unknown",
                String: "string",
                Integer: "integer",
                Decimal: "decimal",
                Datetime: "datetime",
                Boolean: "boolean",
            } as const;
            export type Type = (typeof Type)[keyof typeof Type];
        }
    }

    export type ObjectMappings = ObjectMappings.Item[];

    export namespace ObjectMappings {
        export interface Item {
            response_object_path?: string;
            intercom_object_type?: Item.IntercomObjectType;
            attribute_mappings?: Item.AttributeMappings.Item[];
            reference_mappings?: Item.ReferenceMappings.Item[];
        }

        export namespace Item {
            export const IntercomObjectType = {
                Conversation: "conversation",
                User: "user",
            } as const;
            export type IntercomObjectType = (typeof IntercomObjectType)[keyof typeof IntercomObjectType];
            export type AttributeMappings = AttributeMappings.Item[];

            export namespace AttributeMappings {
                export interface Item {
                    response_attribute_path?: string;
                    intercom_attribute_identifier?: string;
                    mapping_type?: Item.MappingType;
                }

                export namespace Item {
                    export const MappingType = {
                        PrimitiveMapping: "primitive_mapping",
                        ContextMapping: "context_mapping",
                    } as const;
                    export type MappingType = (typeof MappingType)[keyof typeof MappingType];
                }
            }

            export type ReferenceMappings = ReferenceMappings.Item[];

            export namespace ReferenceMappings {
                export interface Item {
                    intercom_object_type?: Item.IntercomObjectType;
                    intercom_attribute_identifier?: string;
                }

                export namespace Item {
                    export const IntercomObjectType = {
                        Conversation: "conversation",
                        User: "user",
                    } as const;
                    export type IntercomObjectType = (typeof IntercomObjectType)[keyof typeof IntercomObjectType];
                }
            }
        }
    }
}
